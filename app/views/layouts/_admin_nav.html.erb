<% if admin? %>
	<div class="login">
		<%=section_link('Products',:controller => 'products', :action => 'index')%>
		<%=section_link('Product Cats',:controller => 'product_cats', :action => 'index')%>
		<%=section_link('Users',:controller => 'users', :action => 'index') %>
		<%= link_to 'Logout', logout_path %>	
		<span><%= current_user.name %> logged in</span>
	</div>
<% end %>
